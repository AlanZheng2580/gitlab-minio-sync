# config-sync component

Reusable GitLab CI/CD component that serializes sync jobs, skips superseded
pipelines, clones every configured private repository and uploads one archive to
S3-compatible MinIO. Release tag: `1.0.0`.
